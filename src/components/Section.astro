---
interface Props {
  id?: string
  class?: string
  noPadding?: boolean
}

const { id, class: className, noPadding } = Astro.props
---

<section id={id} class:list={["sm:mx-4 mt-4 rounded-3xl overflow-hidden", className]}>
  <slot />
</section>

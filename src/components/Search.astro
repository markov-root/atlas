---
import AlgoliaSearch from '@/components/AlgoliaSearch.astro';

interface Props {
  id?: string
  class?: string
  variant?: 'light' | 'dark'
}

const { id = 'search', class: className = '', variant = 'light' } = Astro.props
---

<AlgoliaSearch id={id} class={className} variant={variant} />

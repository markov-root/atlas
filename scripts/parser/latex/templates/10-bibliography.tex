% File: scripts/latex/templates/10-bibliography.tex
% Bibliography and citation settings

% Use plain bibliography style instead of natbib to avoid conflicts
% Since we're using hyperlinked citations in the text, we just need
% a simple bibliography format

% No special citation package needed since we're not using \cite commands
% All citations are handled as hyperlinks in the text
